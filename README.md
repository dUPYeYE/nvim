## Install

`
git clone https://github.com/dUPYeYE/nvim ~/.config/nvim --depth 1 && nvim
`

## Uninstall
`
    # Linux / Macos (unix)
    rm -rf ~/.config/nvim
    rm -rf ~/.local/share/nvim

    # Windows
    rd -r ~\AppData\Local\nvim
    rd -r ~\AppData\Local\nvim-data
`
